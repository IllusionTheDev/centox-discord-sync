import { SubmissionCommentedEvent } from "../types/SubmissionCommentedEvent";
import { Client, Guild } from "discord.js";
 
export default async function handle(client: Client, guild: Guild, event: SubmissionCommentedEvent){

    // This event is not handled
}