import { CommentRepliedEvent } from "../types/CommentRepliedEvent";
import { Client, Guild } from "discord.js";

export default async function handle(client: Client, guild: Guild, event: CommentRepliedEvent){

    // This event is not handled
}